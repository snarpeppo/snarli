{
  "name": "snarli",
  "version": "1.0.0",
  "description": "snarli cli",
  "license": "ISC",
  "author": "",
  "main": "dist/index.js",
  "bin": {
    "snarli": "./dist/index.js"
  },
  "type": "module",
  "scripts": {
    "build": "tsc",
    "build:sea": "npm run build && esbuild dist/index.js --bundle --platform=node --outfile=dist/index.cjs --format=cjs --metafile=dist/index.meta.json",
    "dev": "tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "chalk": "^5.5.0",
    "commander": "^14.0.0",
    "figlet": "^1.8.2",
    "inquirer": "^8.2.4",
    "inquirer-tree-prompt": "^1.1.2",
    "ora": "^8.2.0",
    "systeminformation": "^5.27.11"
  },
  "devDependencies": {
    "@types/chalk": "^0.4.31",
    "@types/inquirer": "^9.0.9",
    "@types/node": "^25.0.3",
    "esbuild": "^0.25.8",
    "typescript": "^5.9.3"
  }
}
